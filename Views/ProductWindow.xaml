<Window
    x:Class="WPFBoilerPlate.Views.ProductWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPFBoilerPlate.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WPFBoilerPlate.ViewModels"
    Title="ProductWindow"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance vm:ProductViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Label
                Grid.Row="0"
                Content="Category"
                Target="{Binding ElementName=CategoryNameTextBox}" />
            <ComboBox
                x:Name="CategoryNameTextBox"
                Grid.Row="0"
                Grid.Column="1"
                DisplayMemberPath="Name"
                IsReadOnly="True"
                ItemsSource="{Binding Categories}"
                SelectedItem="{Binding Category, Mode=TwoWay}" />

            <Label
                Grid.Row="1"
                Content="Name"
                Target="{Binding ElementName=NameTextBox}" />
            <TextBox
                x:Name="NameTextBox"
                Grid.Row="1"
                Grid.Column="1"
                Text="{Binding Name, Mode=TwoWay}" />
        </Grid>

        <UniformGrid Grid.Row="1" HorizontalAlignment="Center">
            <Button Command="{Binding UpdateProductCommand}" Content="Update" />
            <Button Command="{Binding DeleteProductCommand}" Content="Delete" />
        </UniformGrid>

    </Grid>
</Window>
